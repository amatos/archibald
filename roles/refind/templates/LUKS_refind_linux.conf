"Regular Boot" "initrd=\initramfs-%v.img {{ 'cryptdevice=UUID=' + LUKS.cryptdevice_uuid + ':' + LUKS.cryptdevice_container if LUKS.cryptdevice_uuid and LUKS.cryptdevice_container else '' }} {{ 'root=' + LUKS.root if LUKS.root else '' }} {{ 'resume=' + LUKS.resume if LUKS.resume else '' }} {{ LUKS.additional_kernel_params }}"
"Fallback Boot" "initrd=\initramfs-%v-fallback.img {{ 'cryptdevice=UUID=' + LUKS.cryptdevice_uuid + ':' + LUKS.cryptdevice_container if LUKS.cryptdevice_uuid and LUKS.cryptdevice_container else '' }} {{ 'root=' + LUKS.root if LUKS.root else '' }} {{ 'resume=' + LUKS.resume if LUKS.resume else '' }} {{ LUKS.additional_kernel_params }}"
